# Service account for deepseek-app-day9.service.
#
#   sudo cp deploy/deepseek-app.sysusers.conf /etc/sysusers.d/deepseek-app.conf
#   sudo systemd-sysusers
#   id deepseek-app
#
# Creates the system user AND the group of the same name, no login shell.
#Type Name          ID  GECOS                 Home  Shell
u     deepseek-app  -   "DeepSeek chat agent" -     /usr/sbin/nologin
