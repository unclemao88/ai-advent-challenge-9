node_modules/
npm-debug.log*
*.log

# Local secrets — the API key comes from the environment, never from the repo
.env
.env.*
*.env
# ...except the committed template, which holds no secrets
!.env.example

.DS_Store
